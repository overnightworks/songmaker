sonar.organization=overnightworks
sonar.projectKey=overnightworks_songmaker
sonar.sources=src,frontend/src
sonar.tests=tests,frontend
sonar.test.inclusions=tests/**,frontend/src/**/*.test.ts,frontend/e2e/**
sonar.exclusions=vendor/**,docs/design/**,frontend/e2e/test-results/**,frontend/playwright-report/**,plans/**,**/node_modules/**,**/.svelte-kit/**,frontend/build/**
sonar.python.version=3.12
sonar.sourceEncoding=UTF-8

# The `sonar` job in .github/workflows/ci.yml downloads these reports from
# the backend and frontend jobs before scanning (#679).
sonar.python.coverage.reportPaths=reports/coverage.xml
sonar.javascript.lcov.reportPaths=reports/frontend-coverage/lcov.info

# Keep these coverage exclusions identical to .github/workflows/coveragerc-ci. Each exception is
# limited to the named path and has a reason rather than hiding testable code.
# why: src/songmaker_cli/main.py is a CLI entry point with local recovery commands.
# why: src/songmaker_cli/scoring/audiobox_aesthetics.py needs a heavy model runtime absent from CI.
# why: src/songmaker_cli/scoring/emotional_dynamics.py needs a heavy model runtime absent from CI.
# why: src/songmaker_cli/scoring/lyrical_coherence.py requires an external provider runtime.
# why: src/songmaker_cli/scoring/text_accuracy.py requires the Whisper model absent from CI.
# why: src/songmaker_cli/db/migrations/versions/** requires Alembic/PostgreSQL semantics.
# why: frontend/src/service-worker.ts belongs to a browser/build proof, not Vitest coverage.
sonar.coverage.exclusions=src/songmaker_cli/main.py,src/songmaker_cli/scoring/audiobox_aesthetics.py,src/songmaker_cli/scoring/emotional_dynamics.py,src/songmaker_cli/scoring/lyrical_coherence.py,src/songmaker_cli/scoring/text_accuracy.py,src/songmaker_cli/db/migrations/versions/**,frontend/src/service-worker.ts

# why: generated API types and immutable database migrations are not maintained
# source; their structural similarity is not a product-code duplication signal.
sonar.cpd.exclusions=frontend/src/lib/api/types.ts,src/songmaker_cli/db/migrations/versions/**

# The scanner cannot infer these project-specific build guarantees (#679).
sonar.issue.ignore.multicriteria=e1,e2,e3,e4,e5

# why: local and vendored projects require their frozen source builds.
sonar.issue.ignore.multicriteria.e1.ruleKey=docker:S8541
sonar.issue.ignore.multicriteria.e1.resourceKey=**/Dockerfile*

# why: uv resolves from the committed, frozen lockfile; required source builds
# are additionally constrained by the lockfile's hashes.
sonar.issue.ignore.multicriteria.e2.ruleKey=docker:S8544
sonar.issue.ignore.multicriteria.e2.resourceKey=**/Dockerfile*

# why: test fixtures intentionally use yield-shaped setup for pytest's fixture
# protocol; without teardown, S9100 adds style noise without product value.
sonar.issue.ignore.multicriteria.e3.ruleKey=python:S9100
sonar.issue.ignore.multicriteria.e3.resourceKey=tests/**

# why: Docker layers are intentionally separate for install, cache, permission,
# and model-warmup boundaries; merging them would reduce operational clarity.
sonar.issue.ignore.multicriteria.e4.ruleKey=docker:S7031
sonar.issue.ignore.multicriteria.e4.resourceKey=**/*Dockerfile*

# why: historical Alembic migrations are immutable deployment artifacts; this
# typing-style rule adds no product or migration-safety value to replayed files.
sonar.issue.ignore.multicriteria.e5.ruleKey=python:S6546
sonar.issue.ignore.multicriteria.e5.resourceKey=src/songmaker_cli/db/migrations/versions/**
